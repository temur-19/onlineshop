def get_userization_scheme_param(
    userization_header_value: str | None,
) -> tuple[str, str]:
    if not userization_header_value:
        return "", ""
    scheme, _, param = userization_header_value.partition(" ")
    return scheme, param.strip()
